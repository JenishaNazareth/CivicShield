'use client'

import Link from 'next/link'
import { useState } from 'react'
import { ArrowLeft, Check, LockKeyhole, ShieldCheck } from 'lucide-react'

export default function SubmitPage() {
  const [submitted, setSubmitted] = useState(false)
  const [token, setToken] = useState('')
  async function handleSubmit(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault()
    const form = new FormData(event.currentTarget)
    const response = await fetch('/api/grievances', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ category: form.get('category'), generalLocation: form.get('generalLocation'), description: form.get('description'), safetyFlag: form.get('safetyFlag') === 'on', priority: form.get('safetyFlag') === 'on' ? 'HIGH' : 'NORMAL' }) })
    const result = await response.json()
    if (!response.ok) return
    setToken(result.trackingToken)
    setSubmitted(true)
  }
  return <main className="min-h-screen bg-background"><header className="border-b border-border"><div className="mx-auto flex max-w-4xl items-center justify-between px-6 py-5"><Link href="/" className="flex items-center gap-3"><span className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground"><LockKeyhole className="size-4" /></span><span className="font-serif text-lg font-semibold">CivicShield</span></Link><Link href="/track" className="text-sm font-medium text-primary hover:underline">Track a grievance</Link></div></header><section className="mx-auto max-w-3xl px-6 py-12"><Link href="/" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" /> Back to overview</Link>{submitted ? <div className="mt-12 rounded-3xl border border-primary/20 bg-card p-8 text-center shadow-sm"><div className="mx-auto flex size-14 items-center justify-center rounded-full bg-secondary text-primary"><Check className="size-7" /></div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.18em] text-primary">Submission received</p><h1 className="mt-3 font-serif text-4xl tracking-tight">Keep this tracking code safe.</h1><p className="mx-auto mt-4 max-w-lg leading-7 text-muted-foreground">This is the only way to return to your grievance without an account. We cannot recover it if it is lost.</p><div className="mx-auto mt-8 max-w-md rounded-2xl bg-muted p-5 font-mono text-xl font-semibold tracking-wider">{token}</div><Link href="/track" className="mt-8 inline-flex rounded-full bg-primary px-6 py-3 text-sm font-semibold text-primary-foreground">Go to tracking portal</Link></div> : <><div className="mt-10"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-primary">Private submission</p><h1 className="mt-3 font-serif text-5xl tracking-tight">Tell us what happened.</h1><p className="mt-4 max-w-2xl leading-7 text-muted-foreground">You may submit anonymously. Your description is encrypted before it is stored and is only available to the assigned investigator.</p></div><div className="mt-8 flex gap-3 rounded-2xl border border-primary/20 bg-primary/5 p-4 text-sm leading-6"><ShieldCheck className="mt-0.5 size-5 shrink-0 text-primary" /><p><strong>Privacy boundary:</strong> this form does not ask for your name, student ID, IP-derived identity, or any unnecessary personal information.</p></div><form onSubmit={handleSubmit} className="mt-8 flex flex-col gap-6 rounded-3xl border border-border bg-card p-6 shadow-sm"><label className="flex flex-col gap-2 text-sm font-medium">Concern category<select name="category" required className="rounded-xl border border-input bg-background px-4 py-3 font-normal"><option value="">Choose a category</option><option>Academic integrity</option><option>Harassment or discrimination</option><option>Safety and facilities</option><option>Administration</option><option>Other</option></select></label><label className="flex flex-col gap-2 text-sm font-medium">Where did this happen? <span className="font-normal text-muted-foreground">Use a general area, not a precise location.</span><input name="generalLocation" required className="rounded-xl border border-input bg-background px-4 py-3 font-normal" placeholder="e.g. North campus / residence hall" /></label><label className="flex flex-col gap-2 text-sm font-medium">Describe your concern<textarea name="description" required minLength={20} className="min-h-40 rounded-xl border border-input bg-background px-4 py-3 font-normal" placeholder="Share what you feel is important for the investigator to understand." /></label><label className="flex items-center gap-3 rounded-xl bg-muted/70 p-4 text-sm"><input name="safetyFlag" type="checkbox" className="size-4 accent-primary" /> I believe someone may be in immediate danger.</label><button type="submit" className="rounded-full bg-primary px-6 py-3.5 text-sm font-semibold text-primary-foreground">Encrypt and submit grievance</button><p className="text-center text-xs leading-5 text-muted-foreground">By submitting, you understand that anonymity has practical limits. Please avoid including identifying details unless necessary for the investigation.</p></form></>}</section></main>
}
